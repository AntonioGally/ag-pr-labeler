name: "PR labeler - commit based"
description: "Add labels to your PR based in commits"
author: AntonioGally
branding:
  icon: file-text
  color: purple
inputs:
  githubToken:
    description: "Github default token"
    required: true
  createRepoLabels:
    description: "Create required labels to your repository"
    required: false

runs:
  using: docker
  image: "Dockerfile"
